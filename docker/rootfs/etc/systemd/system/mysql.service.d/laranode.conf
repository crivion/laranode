# Initialise an empty datadir (fresh volume) before Ubuntu's own pre-start checks run
[Service]
ExecStartPre=
ExecStartPre=/usr/local/sbin/laranode-mysql-init
ExecStartPre=/usr/share/mysql/mysql-systemd-start pre
